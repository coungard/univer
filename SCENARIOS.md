# Сценарии — Univer Backend

> Пошаговые прикладные сценарии работы с REST API: `curl`-запросы к приложению на `http://localhost:8023`
> (Keycloak на `8082`, Postgres на `5435` — `docker-compose.yml`). ID сущностей, которые уже существуют в
> dev-БД до начала сценария (`ДГТУ`, факультет, программа, кафедра, преподаватель — из
> `V2__insert_universities_data.sql` / `V3__insert_teachers_data.sql`), настоящие. Каждый сценарий ниже
> реально прогонялся и был проверен против живого приложения и Postgres (Сценарии 1–3 — 20.08.2026,
> Сценарий 4 — 24.08.2026), но созданные им данные (`StudyYear`, `Semester`, `Group`, `Student`,
> `WeekScheduleCycle`, `Course`, `Pair`, `Lecture`) впоследствии удалены из dev-БД для чистоты — поэтому их
> ID в примерах ответов ниже заменены на моковые плейсхолдеры (`11111111-...`, `22222222-...` и т.д.), не
> связанные с текущим состоянием БД. При повторном прогоне сценария реальные ID в ответах API будут
> другими — сама механика (эндпоинты, поля, порядок шагов, найденные по пути баги и особенности) проверена
> и актуальна. **Исключение:** к моменту прогона Сценария 4 миграции `V15`/`V16` (полное реальное
> расписание КТиЭ) уже сделали цепочку `StudyYear`/`Semester`/`Group`/`Pair`, которую создают Сценарии 1 и
> 3, частью постоянных сид-данных — см. врезку в начале Сценария 4.

---

## Сценарий 1: Создание групп 1 курса РПиС (очное отделение)

**Цель:** создать две студенческие группы первого курса по программе «Разработка программно-информационных
систем» (РПиС, код `09.03.04`, очная форма обучения) — `У533 РПИС` и `У530 РПИС` — привязанные к одной и
той же программе и через неё к Факультету компьютерных технологий и энергетики.

Группа (`Group`) не привязывается к факультету и программе напрямую — связь идёт по цепочке
`Group → Semester → StudyYear → Program → Faculty` (см. `TARGET.md`, раздел «Циклическое расписание»).
Поэтому прежде чем создать саму группу, нужно создать курс обучения (`StudyYear`, номер 1) и семестр
(`Semester`) для нужной программы — если их ещё нет.

### Исходные данные (уже есть в БД)

| Сущность | Значение | ID |
|---|---|---|
| Университет | ДГТУ | `796c5ad4-52ba-482f-a3bb-31c5de38762d` |
| Факультет | Факультет компьютерных технологий и энергетики | `9980825f-f630-426a-ac6a-5a185b30f944` |
| Программа | 09.03.04 «Программная инженерия» (направленность «Разработка программно-информационных систем», РПиС), **очная**, 4 года | `0812c171-3317-4ee2-bc3c-cba404fd53d6` |

У программы `09.03.04` в БД есть и заочный вариант (5 лет, другой ID) — для сценария важно использовать
именно очный (`education_form: FULL_TIME`), как указано в задаче.

### Шаг 0. Получить access-токен ADMIN

Через Swagger UI (`http://localhost:8023/swagger-ui/index.html` → Authorize → username/password, см.
настройку в этой же сессии) либо напрямую у Keycloak (password grant, клиент публичный секрета не
требует):

```bash
curl -s -X POST "http://localhost:8082/realms/univer-realm/protocol/openid-connect/token" \
  -d "client_id=univer-client" \
  -d "client_secret=univer-client-secret" \
  -d "grant_type=password" \
  -d "username=super" \
  -d "password=1234" \
  -d "scope=openid" \
  | jq -r .access_token
```

Дальше `<ACCESS_TOKEN>` — значение из ответа. Все POST-эндпоинты ниже требуют роль `ADMIN`.

### Шаг 1. Создать курс обучения (`StudyYear`) — 1 курс программы РПиС

```bash
curl -s -X POST "http://localhost:8023/api/v1/study-years" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
        "programId": "0812c171-3317-4ee2-bc3c-cba404fd53d6",
        "yearNumber": 1
      }'
```

Пример ответа (`201 Created`):

```json
{
  "id": "11111111-1111-1111-1111-111111111111",
  "programId": "0812c171-3317-4ee2-bc3c-cba404fd53d6",
  "yearNumber": 1
}
```

### Шаг 2. Создать семестр (`Semester`) — осенний семестр 1 курса

```bash
curl -s -X POST "http://localhost:8023/api/v1/semesters" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
        "studyYearId": "11111111-1111-1111-1111-111111111111",
        "type": "AUTUMN",
        "startDate": "2026-09-01"
      }'
```

Пример ответа (`201 Created`):

```json
{
  "id": "22222222-2222-2222-2222-222222222222",
  "studyYearId": "11111111-1111-1111-1111-111111111111",
  "type": "AUTUMN",
  "startDate": "2026-09-01"
}
```

`startDate` важен не только как дата начала — от него считается чётность недели циклического
расписания (`WeekScheduleCycle`/`Pair`, Этап 3), если для этой группы позже понадобится расписание.

### Шаг 3. Создать группу (`Group`)

```bash
curl -s -X POST "http://localhost:8023/api/v1/groups" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
        "semesterId": "22222222-2222-2222-2222-222222222222",
        "name": "У533 РПИС"
      }'
```

Пример ответа (`201 Created`):

```json
{
  "id": "33333333-3333-3333-3333-333333333333",
  "semesterId": "22222222-2222-2222-2222-222222222222",
  "name": "У533 РПИС"
}
```

`name` — свободная строка (см. `GroupDto`), связи с факультетом/программой/курсом — источник истины, не
текст в названии; `У533 РПИС` здесь читается как «РПиС, 1 курс, группа 1» по аналогии с существующими
тестовыми данными (`У532 КСиТ`), а не как зафиксированный в проекте формат.

> **Примечание для Windows (Git Bash/MINGW).** curl-команда выше с инлайновым `-d '{ ... кириллица ... }'`
> при прогоне 20.08.2026 упала `400 Bad Request` — тело с кириллицей внутри одинарных кавычек уходит не в
> UTF-8 (кодировка консоли/шелла, а не баг Spring/Jackson на стороне сервера). Обходной путь — сохранить
> JSON в файл в явном UTF-8 и отправить через `--data-binary @file`:
> ```bash
> printf '{"semesterId": "22222222-2222-2222-2222-222222222222", "name": "\xd0\xa3533 \xd0\xa0\xd0\x9f\xd0\x98\xd0\xa1"}' > group.json
> curl -s -X POST "http://localhost:8023/api/v1/groups" \
>   -H "Authorization: Bearer <ACCESS_TOKEN>" \
>   -H "Content-Type: application/json; charset=utf-8" \
>   --data-binary @group.json
> ```
> После этого запрос отработал (`201 Created`, `name` вернулось корректно). То же стоит иметь в виду для
> любого другого запроса в этом документе с кириллицей в теле — не только для Шага 3.

### Шаг 4. Проверить результат

```bash
curl -s "http://localhost:8023/api/v1/groups/33333333-3333-3333-3333-333333333333" \
  -H "Authorization: Bearer <ACCESS_TOKEN>"
```

или список групп семестра:

```bash
curl -s "http://localhost:8023/api/v1/groups/semester/22222222-2222-2222-2222-222222222222?page=0&size=10" \
  -H "Authorization: Bearer <ACCESS_TOKEN>"
```

### Шаг 5. Создать вторую группу той же программы (`У530 РПИС`)

На один курс программы обычно набирают несколько групп — `Group` привязывается к `Semester`, а не
один-к-одному, поэтому второй `POST /api/v1/groups` с тем же `semesterId`, что и у `У533 РПИС`, создаёт
ещё одну группу на той же цепочке `Semester → StudyYear → Program`, без повторения Шагов 1–2.

```bash
curl -s -X POST "http://localhost:8023/api/v1/groups" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
        "semesterId": "22222222-2222-2222-2222-222222222222",
        "name": "У530 РПИС"
      }'
```

Пример ответа (`201 Created`):

```json
{
  "id": "44444444-4444-4444-4444-444444444444",
  "semesterId": "22222222-2222-2222-2222-222222222222",
  "name": "У530 РПИС"
}
```

### Шаг 6. Проверить обе группы

```bash
curl -s "http://localhost:8023/api/v1/groups/semester/22222222-2222-2222-2222-222222222222?page=0&size=10" \
  -H "Authorization: Bearer <ACCESS_TOKEN>"
```

Пример ответа (`200 OK`, сокращено до `content`):

```json
{
  "content": [
    {
      "id": "33333333-3333-3333-3333-333333333333",
      "semesterId": "22222222-2222-2222-2222-222222222222",
      "name": "У533 РПИС"
    },
    {
      "id": "44444444-4444-4444-4444-444444444444",
      "semesterId": "22222222-2222-2222-2222-222222222222",
      "name": "У530 РПИС"
    }
  ],
  "totalElements": 2
}
```

Обе группы — с одним и тем же `semesterId`; при реальном прогоне 20.08.2026 это дополнительно было
подтверждено JOIN'ом `student_groups → semesters → study_years` в Postgres — у обеих строк был один и тот
же `program_id` (`0812c171-3317-4ee2-bc3c-cba404fd53d6`).

### Итог

Создана цепочка `Program (09.03.04, очно) → StudyYear (курс 1) → Semester (осень 2026)`, на которой висят
две группы — `Group (У533 РПИС)` и `Group (У530 РПИС)` — обе привязаны через одну и ту же программу к
Факультету компьютерных технологий и энергетики. Группы готовы к дальнейшим сценариям Этапа 3–5: привязка
`Student` к группе (`group_id`), циклическое расписание (`WeekScheduleCycle`/`Pair`), массовое зачисление
группы на курс (`POST /api/v1/enrollments/group/{groupId}/course/{courseId}`, Этап 5).

---

## Сценарий 2: Регистрация студента и привязка к группе из Сценария 1

**Цель:** зарегистрировать нового студента и привязать его к группе `У533 РПИС`, созданной в Сценарии 1.

Сценарий прогонялся 20.08.2026 против живого приложения и был проверен прямым SQL-запросом к Postgres;
созданные им данные с тех пор удалены из dev-БД — ID и тела ответов ниже приведены в том же формате, что
вернул реальный прогон (моковые плейсхолдеры вместо удалённых реальных ID).

### Исходные данные (уже есть в БД / из Сценария 1)

| Сущность | Значение | ID |
|---|---|---|
| Университет | ДГТУ | `796c5ad4-52ba-482f-a3bb-31c5de38762d` |
| Группа | У533 РПИС (Сценарий 1) | `33333333-3333-3333-3333-333333333333` |

### Шаг 0. Получить access-токен ADMIN

См. Сценарий 1, Шаг 0. Токен нужен только для Шага 2 (`PUT /students/{id}` требует роль `ADMIN`) — сама
регистрация (Шаг 1) публична и токена не требует.

### Шаг 1. Зарегистрировать студента

`POST /api/v1/students/register` — публичный эндпоинт (`permitAll()` в `SecurityConfig`), доступен без
токена. Регистрация создаёт пользователя в Keycloak (роль `ROLE_STUDENT`) и локальную сущность `Student`
с тем же ID, что и Keycloak user ID (см. `CLAUDE.md`, «Флоу регистрации»).

```bash
curl -s -X POST "http://localhost:8023/api/v1/students/register" \
  -H "Content-Type: application/json; charset=utf-8" \
  -d '{
        "username": "ivanov.i",
        "firstname": "Иван",
        "lastname": "Иванов",
        "fullname": "Иванов Иван Иванович",
        "email": "ivanov.i@dstu-student.ru",
        "password": "P@ssw0rd123",
        "enrollmentDate": "2026-08-15",
        "universityId": "796c5ad4-52ba-482f-a3bb-31c5de38762d"
      }'
```

> На Windows/Git Bash см. примечание про кодировку кириллицы у Шага 3 Сценария 1 — тот же приём
> (`--data-binary @file` с явным UTF-8) применим и здесь.

Пример ответа (`201 Created`):

```json
{
  "id": "55555555-5555-5555-5555-555555555555",
  "username": "ivanov.i",
  "firstname": "Иван",
  "lastname": "Иванов",
  "fullname": "Иванов Иван Иванович",
  "createdAt": "2026-08-20T08:53:28.861184800Z",
  "updatedAt": "2026-08-20T08:53:28.861184800Z",
  "email": "ivanov.i@dstu-student.ru",
  "enrollmentDate": "2026-08-15",
  "universityId": "796c5ad4-52ba-482f-a3bb-31c5de38762d",
  "groupId": null
}
```

> **Исправленный баг (20.08.2026).** `StudentMapper.toDto` не мапил `enrollmentDate` в `StudentDto` —
> любой ответ со студентом (`register`/`GET`/`PUT`) отдавал `enrollmentDate: null`, хотя в БД значение
> сохранялось верно. Это ломало естественный workflow «получить студента → переслать тело с добавленным
> `groupId`»: наивный `PUT` в Шаге 2 падал `400 {"enrollmentDate":"Дата зачисления обязательна"}`,
> потому что `StudentDto.enrollmentDate` помечено `@NotNull`. Исправлено — `StudentMapper.toDto` теперь
> явно мапит `.enrollmentDate(student.getEnrollmentDate())`. Ответ выше — уже с исправлением; `groupId`
> пока `null`, так как связь с группой выставляется отдельным шагом ниже.

### Шаг 2. Привязать студента к группе

Прямого эндпоинта «привязать студента к группе» нет — связь выставляется через `PUT /api/v1/students/{id}`
(требует `hasRole('ADMIN')`) с полным телом `StudentDto`, где `groupId` — единственное новое поле;
остальные поля берутся из ответа Шага 1. `PUT` — не PATCH: все `@NotNull`/`@NotBlank` поля `StudentDto`
обязательны, включая `username` — хотя `StudentServiceImpl.updateStudent` его валидирует по DTO, но
фактически не применяет к сущности (обновляются только `firstname`/`lastname`/`fullname`/`email`/
`enrollmentDate`/`university`/`group`; логин менять через этот эндпоинт нельзя).

```bash
curl -s -X PUT "http://localhost:8023/api/v1/students/55555555-5555-5555-5555-555555555555" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json; charset=utf-8" \
  -d '{
        "username": "ivanov.i",
        "firstname": "Иван",
        "lastname": "Иванов",
        "fullname": "Иванов Иван Иванович",
        "email": "ivanov.i@dstu-student.ru",
        "enrollmentDate": "2026-08-15",
        "universityId": "796c5ad4-52ba-482f-a3bb-31c5de38762d",
        "groupId": "33333333-3333-3333-3333-333333333333"
      }'
```

Пример ответа (`200 OK`):

```json
{
  "id": "55555555-5555-5555-5555-555555555555",
  "username": "ivanov.i",
  "firstname": "Иван",
  "lastname": "Иванов",
  "fullname": "Иванов Иван Иванович",
  "createdAt": "2026-08-20T08:53:28.861185Z",
  "updatedAt": "2026-08-20T08:53:40.605967Z",
  "email": "ivanov.i@dstu-student.ru",
  "enrollmentDate": "2026-08-15",
  "universityId": "796c5ad4-52ba-482f-a3bb-31c5de38762d",
  "groupId": "33333333-3333-3333-3333-333333333333"
}
```

### Шаг 3. Проверить результат

`GET /api/v1/students/{id}` требует роль `STUDENT` (не `ADMIN`) — токеном ADMIN его не вызвать (403).
Для проверки под ADMIN удобнее списковый эндпоинт:

```bash
curl -s "http://localhost:8023/api/v1/students?page=0&size=10" \
  -H "Authorization: Bearer <ACCESS_TOKEN>"
```

В ответе у нужного студента `"groupId": "33333333-3333-3333-3333-333333333333"`.

### Итог

Зарегистрирован студент Иван Иванов (`ivanov.i`, `id = 55555555-5555-5555-5555-555555555555`) через
`POST /students/register` (Keycloak-пользователь + локальная сущность с тем же ID) и привязан к группе
`У533 РПИС` из Сценария 1 через `PUT /students/{id}` с `groupId`. Заодно найден и исправлен баг маппинга
`enrollmentDate` в `StudentMapper`, ломавший естественный round-trip «прочитать → дозаполнить → отправить»
для любого клиента Student API.

---

## Сценарий 3: Пара №1 «История России» по понедельникам нечётной недели для двух групп

**Цель:** создать шаблон циклического расписания (`Pair`) №1: понедельник, нечётная неделя (1, 3, 5-я...
неделя семестра), учебный курс «История России» в кабинете 125, преподаватель — Казакбиева Ольга
Ивановна, поточно для обеих групп 1 курса РПиС из Сценария 1 (`У533 РПИС` и `У530 РПИС`).

Сценарий прогонялся 20.08.2026 против живого приложения и был проверен прямым SQL-запросом к Postgres;
созданные им данные с тех пор удалены из dev-БД — ID и тела ответов ниже приведены в том же формате, что
вернул реальный прогон (моковые плейсхолдеры вместо удалённых реальных ID).

> **Добавлено поле `room` (20.08.2026).** До этого сценария в модели не было понятия «аудитория» —
> ни у `Pair`, ни у `Lecture` (см. `TARGET.md`, где `Room` зафиксирована как `❌ не реализовано —
> целевая сущность`, `Pair }o--o| Room`). Чтобы записать «кабинет 125», добавлено простое опциональное
> строковое поле `room` на `Pair` (миграция `V12__add_room_to_pairs_and_lectures.sql`) и на `Lecture` —
> при генерации лекции из `Pair` (`POST /lectures/generate`) `room` пробрасывается автоматически.
> Полноценная сущность `Room` (вместимость, конфликты бронирования и т.п.) из ER-диаграммы `TARGET.md` —
> задел на будущее, не реализована.

### Исходные данные (из Сценария 1 / уже есть в БД)

| Сущность | Значение | ID |
|---|---|---|
| Семестр | Осенний 2026, 1 курс РПиС (Сценарий 1) | `22222222-2222-2222-2222-222222222222` |
| Группа | У533 РПИС (Сценарий 1) | `33333333-3333-3333-3333-333333333333` |
| Группа | У530 РПИС (Сценарий 1) | `44444444-4444-4444-4444-444444444444` |
| Кафедра | Кафедра гуманитарных наук | `d76edda8-f468-484d-8fa4-e0a2f52c9092` |
| Преподаватель | Казакбиева Ольга Ивановна, ст. преподаватель, к.и.н. | `d05c213f-efcf-4327-9e1c-65455fa68845` |

Преподаватель уже существует в dev-БД (`V3__insert_teachers_data.sql`) — не регистрируется заново.

### Шаг 0. Получить access-токен ADMIN

См. Сценарий 1, Шаг 0.

### Шаг 1. Создать циклическое расписание семестра (`WeekScheduleCycle`)

Один `WeekScheduleCycle` на семестр (не на группу) — для семестра из Сценария 1 его ещё нет.

```bash
curl -s -X POST "http://localhost:8023/api/v1/week-schedule-cycles" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"semesterId": "22222222-2222-2222-2222-222222222222"}'
```

Пример ответа (`201 Created`):

```json
{
  "id": "66666666-6666-6666-6666-666666666666",
  "semesterId": "22222222-2222-2222-2222-222222222222"
}
```

### Шаг 2. Создать учебный курс «История России»

`Pair` ссылается на `Course`, а не на голое название — курса «История России» в БД ещё нет, создаём с
привязкой к кафедре гуманитарных наук и преподавателю Казакбиевой.

```bash
curl -s -X POST "http://localhost:8023/api/v1/courses" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json; charset=utf-8" \
  -d '{
        "title": "История России",
        "departmentId": "d76edda8-f468-484d-8fa4-e0a2f52c9092",
        "teacherId": "d05c213f-efcf-4327-9e1c-65455fa68845"
      }'
```

Пример ответа (`201 Created`):

```json
{
  "id": "77777777-7777-7777-7777-777777777777",
  "title": "История России",
  "departmentId": "d76edda8-f468-484d-8fa4-e0a2f52c9092",
  "teacherId": "d05c213f-efcf-4327-9e1c-65455fa68845"
}
```

### Шаг 3. Создать пару (`Pair`) №1

Понедельник (`dayOfWeek: MONDAY`), нечётная неделя (`weekParity: ODD` — недели 1, 3, 5...), поточно для
обеих групп сразу (`groupIds` — обе группы, `Pair` ↔ `Group` многие-ко-многим, см. `TARGET.md`, раздел
«Поток»). Время пары не было задано в задаче — взято по аналогии с иллюстративным примером `TARGET.md`
(1-я пара, 08:00–09:30).

```bash
curl -s -X POST "http://localhost:8023/api/v1/pairs" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
        "weekScheduleCycleId": "66666666-6666-6666-6666-666666666666",
        "dayOfWeek": "MONDAY",
        "weekParity": "ODD",
        "pairNumber": 1,
        "startTime": "08:00:00",
        "endTime": "09:30:00",
        "courseId": "77777777-7777-7777-7777-777777777777",
        "teacherId": "d05c213f-efcf-4327-9e1c-65455fa68845",
        "room": "125",
        "groupIds": ["33333333-3333-3333-3333-333333333333", "44444444-4444-4444-4444-444444444444"]
      }'
```

Пример ответа (`201 Created`):

```json
{
  "id": "88888888-8888-8888-8888-888888888888",
  "weekScheduleCycleId": "66666666-6666-6666-6666-666666666666",
  "dayOfWeek": "MONDAY",
  "weekParity": "ODD",
  "pairNumber": 1,
  "startTime": "08:00:00",
  "endTime": "09:30:00",
  "courseId": "77777777-7777-7777-7777-777777777777",
  "teacherId": "d05c213f-efcf-4327-9e1c-65455fa68845",
  "room": "125",
  "groupIds": ["44444444-4444-4444-4444-444444444444", "33333333-3333-3333-3333-333333333333"]
}
```

При реальном прогоне 20.08.2026 это дополнительно было сверено напрямую в БД:
`pairs ⋈ courses ⋈ teachers ⋈ pair_groups ⋈ student_groups` — у обеих строк (по группе на строку из-за
join) был один и тот же `room = 125`, `course = 'История России'`, `teacher.person_id` = person
Казакбиевой Ольги Ивановны.

### Шаг 4. Сгенерировать лекцию из пары на конкретную дату (опционально, для проверки)

Показывает, что `room` доходит до конкретной лекции при генерации из шаблона (`POST /lectures/generate`,
Этап 4). Дата подобрана так, чтобы день недели и чётность недели совпадали с параметрами пары: семестр
начинается `2026-09-01` (вторник), первый понедельник семестра — `2026-09-07`, попадает в 1-ю (нечётную)
неделю.

```bash
curl -s -X POST "http://localhost:8023/api/v1/lectures/generate" \
  -H "Authorization: Bearer <ACCESS_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"pairId": "88888888-8888-8888-8888-888888888888", "date": "2026-09-07"}'
```

Пример ответа (`201 Created`):

```json
{
  "id": "99999999-9999-9999-9999-999999999999",
  "title": "История России",
  "content": null,
  "scheduledTime": "2026-09-07T08:00:00",
  "durationMinutes": 90,
  "courseId": "77777777-7777-7777-7777-777777777777",
  "teacherId": "d05c213f-efcf-4327-9e1c-65455fa68845",
  "room": "125",
  "sourcePairId": "88888888-8888-8888-8888-888888888888",
  "groupIds": ["44444444-4444-4444-4444-444444444444", "33333333-3333-3333-3333-333333333333"]
}
```

### Итог

Создана пара `Pair` №1 «История России» (`id = 88888888-8888-8888-8888-888888888888`): понедельник,
нечётная неделя, 08:00–09:30, кабинет 125, преподаватель Казакбиева О.И., поточно для `У533 РПИС` и
`У530 РПИС`. По пути в модель добавлено поле `room` (`Pair` и `Lecture`), которого раньше не было вовсе.
Дополнительно продемонстрирована генерация конкретной лекции из этого шаблона на `2026-09-07` — `room`
пробрасывается автоматически, что закрывает Этап 4 сквозным примером «шаблон → конкретное занятие».

---

## Сценарий 4: Студент видит свои занятия за осенний семестр

**Цель:** студент Иван Иванов (`ivanov.i`, зарегистрирован и привязан к группе `У533 РПИС` в Сценарии 2)
своим собственным токеном (роль `STUDENT`, не `ADMIN`) получает список всех своих занятий осеннего
семестра.

Сценарий прогнан против живого приложения 24.08.2026 (Docker с Postgres/Keycloak поднят) и подтверждён
прямым SQL-запросом к Postgres — как и Сценарии 1–3.

> **Важный нюанс, обнаруженный при живом прогоне.** С момента, когда писались Сценарии 1–3 (20.08.2026),
> в dev-БД миграциями `V15__insert_ktie_full_week_schedule.sql`/
> `V16__insert_ktie_full_week_schedule_even.sql` появилось постоянное (не удаляемое между сессиями)
> реальное расписание КТиЭ — в том числе ровно тот же `StudyYear` (программа РПиС `0812c171-...`, курс 1),
> `Semester` (осень) и группы `У533 РПиС`/`У530 РПиС`, которые Сценарий 1 создаёт с нуля. Буквальный
> повтор Сценария 1, Шага 1 сейчас падает `422 {"message":"Курс 1 уже существует для программы с ID:
> 0812c171-..."}` — это подтверждает, что `StudyYearServiceImpl.validateUnique` действительно работает,
> но означает, что Сценарий 1 больше не воспроизводим с нуля на этой dev-БД. Для этого прогона Сценария 4
> вместо повторного создания цепочки `StudyYear → Semester → Group` (Сценарий 1) и `Pair`
> «История России» (Сценарий 3) переиспользована уже существующая реальная цепочка из V15 — она
> функционально идентична тому, что описывают Сценарии 1 и 3 (та же программа, тот же курс, те же две
> группы, тот же предмет и преподаватель поточно на понедельник нечётной недели), просто с другими
> (настоящими, стабильными) ID. Создан и сразу удалён после проверки был только тестовый
> студент+лекция — см. «Реальные ID этого прогона» ниже.

Прямого эндпоинта «расписание текущего студента» (условный `GET /students/me/lectures`) в API нет.
Единственный путь — эндпоинт `GET /lectures/group/{groupId}`, для которого студенту нужно знать
собственный `groupId`; у него нет отдельного `@PreAuthorize`, поэтому подходит любая аутентифицированная
роль (`ADMIN`/`TEACHER`/`STUDENT` — `SecurityConfig` требует лишь `anyRequest().authenticated()`).

> **Обновление:** этот пробел закрыт — добавлен `GET /api/v1/lectures/me` (`LectureController`),
> `@PreAuthorize("hasRole('STUDENT')")`. ID студента берётся из `jwt.getSubject()` (Keycloak subject =
> `Student.id`, см. флоу регистрации в `CLAUDE.md`), группа разрешается через него, лекции
> возвращаются отсортированными по `scheduledTime`. Путь `/lectures/me`, а не `/students/me/lectures`,
> выбран ради соответствия инварианту проекта «1 контроллер : 1 сервис» — ресурс, который отдаётся,
> это `Lecture`, а не `Student`.

### Исходные данные (из Сценариев 2–3, моковые плейсхолдеры — сама форма запросов/ответов не зависит от
### того, что стоит за ID: настоящая постоянная сущность из V15 или разово созданная сценарная)

| Сущность | Значение | ID |
|---|---|---|
| Студент | Иван Иванов (Сценарий 2) | `55555555-5555-5555-5555-555555555555` |
| Группа | У533 РПИС (Сценарий 1, привязана студенту в Сценарии 2) | `33333333-3333-3333-3333-333333333333` |
| Лекция | «История России», 2026-09-07 08:00, поточно У533+У530 (Сценарий 3) | `99999999-9999-9999-9999-999999999999` |

### Реальные ID этого прогона (24.08.2026, для воспроизводимости)

Группа/пара — постоянные ID из V15 (останутся такими же и при следующем прогоне на этой dev-БД); студент
и лекция — созданы для проверки и сразу удалены (`DELETE /students/{id}`, `DELETE /lectures/{id}`), при
следующем прогоне будут другими:

| Сущность | Реальный ID |
|---|---|
| Группа У533 РПиС (из V15) | `3d0f97bd-5c45-4f50-86a8-8506d2e16eeb` |
| Pair «История России», Пн, нечётная неделя, поток У533+У530 (из V15) | `8558192f-b4d8-4077-bfe4-4b25fad33066` |
| Тестовый студент (создан и удалён) | `46deb00e-5034-4c7a-89c9-d29b6ffc2d8b` |
| Тестовая лекция, сгенерирована на `2025-09-01` (создана и удалена) | `bd8ac92d-9204-4d21-940c-d7842dea54f4` |

### Шаг 0. Получить access-токен самого студента

В отличие от Сценариев 1 и 3 — не `ADMIN`, а креды, использованные при регистрации в Сценарии 2 (Шаг 1):

```bash
curl -s -X POST "http://localhost:8082/realms/univer-realm/protocol/openid-connect/token" \
  -d "client_id=univer-client" \
  -d "client_secret=univer-client-secret" \
  -d "grant_type=password" \
  -d "username=ivanov.i" \
  -d "password=P@ssw0rd123" \
  -d "scope=openid" \
  | jq -r .access_token
```

Дальше `<STUDENT_TOKEN>` — значение из ответа; в его `sub` — тот же UUID, что и `id` студента (Keycloak
user ID переиспользован как ID сущности `Student`, см. `CLAUDE.md`, «Флоу регистрации»).

### Шаг 1. (Опционально) Подтвердить свой `groupId`

`GET /students/{id}` требует роль `STUDENT` (см. Сценарий 2, Шаг 3) — эндпоинт не проверяет, что `{id}` в
пути совпадает с `sub` вызывающего токена, но студент и так подставляет собственный ID:

```bash
curl -s "http://localhost:8023/api/v1/students/55555555-5555-5555-5555-555555555555" \
  -H "Authorization: Bearer <STUDENT_TOKEN>"
```

В ответе — `"groupId": "33333333-3333-3333-3333-333333333333"` (выставлен в Сценарии 2, Шаг 2).

### Шаг 2. Получить занятия своей группы

```bash
curl -s "http://localhost:8023/api/v1/lectures/group/33333333-3333-3333-3333-333333333333?page=0&size=10" \
  -H "Authorization: Bearer <STUDENT_TOKEN>"
```

Пример ответа (`200 OK`):

```json
{
  "content": [
    {
      "id": "99999999-9999-9999-9999-999999999999",
      "title": "История России",
      "content": null,
      "scheduledTime": "2026-09-07T08:00:00",
      "durationMinutes": 90,
      "courseId": "77777777-7777-7777-7777-777777777777",
      "teacherId": "d05c213f-efcf-4327-9e1c-65455fa68845",
      "room": "125",
      "sourcePairId": "88888888-8888-8888-8888-888888888888",
      "groupIds": ["33333333-3333-3333-3333-333333333333", "44444444-4444-4444-4444-444444444444"]
    }
  ],
  "totalElements": 1
}
```

`GET /lectures/group/{groupId}` фильтрует только по группе, без параметра семестра/диапазона дат — но в
этой модели `Group` привязана к ровно одному `Semester` (`Group → Semester`, см. Сценарий 1), поэтому все
лекции группы `У533 РПИС` физически не могут относиться ни к какому семестру, кроме осеннего 2026 из
Сценария 1: группо-скоуп здесь автоматически равен семестро-скоупу, отдельная фильтрация по датам не
нужна. Обратная сторона той же связи: если у студента в будущем сменится группа (новый семестр — новая
`Group`), прежние лекции больше не попадут в выборку по текущему `groupId` — историю по всем прошлым
группам эндпоинт не собирает.

> **Замечание про пагинацию.** `page`/`size` — единственные параметры, которые реально влияют на выборку;
> `GroupController`/`LectureController` (и все остальные списковые контроллеры в проекте) строят
> `Pageable` вручную как `PageRequest.of(page, size)`, не читая `sort` из query-параметров — в отличие от
> стандартного Spring Data `Pageable`-резолвера, `?sort=scheduledTime,asc` в URL никак не отсортирует
> результат. При нескольких занятиях в семестре порядок в `content` — как их вернула БД (обычно по
> первичному ключу вставки), а не хронологический; клиенту, которому нужно расписание по датам, придётся
> сортировать `content` самостоятельно после получения страницы.

### Итог

Студент Иван Иванов, используя собственный `STUDENT`-токен (не `ADMIN`), получил список своих занятий —
единственную лекцию «История России» из Сценария 3 — через `GET /lectures/group/{groupId}` со своим
`groupId`. Отдельного «расписания студента» как первоклассного эндпоинта в API нет: связка `Student.group`
(Сценарий 2) → `Group`-скоупный список лекций (уже привязанный к единственному семестру этой группы) —
единственный путь получить «мои занятия за семестр» на текущей модели.

Прогнан 24.08.2026 живьём (тестовый студент `46deb00e-...` из группы `У533 РПиС`, реальная лекция
`bd8ac92d-...`, сгенерированная из постоянного Pair `8558192f-...` из V15 — см. «Реальные ID этого
прогона» выше): оба запроса под `STUDENT`-токеном отработали `200 OK`, `GET /students/{id}` вернул верный
`groupId`, `GET /lectures/group/{groupId}` — ровно 1 лекцию с ожидаемым содержимым; сверено напрямую в
Postgres. Тестовые студент и лекция удалены после проверки (`DELETE /students/{id}`,
`DELETE /lectures/{id}`) — постоянные сущности V15 (группа, пара, курс, преподаватель) не трогались.

## Дополнение к Сценарию 4 (25.08.2026): массовая генерация лекций на весь семестр

Прогон 24.08.2026 выше показывал только одну вручную сгенерированную лекцию — не репрезентативно для
«всех занятий за семестр». После добавления `Semester.endDate` (миграции `V17`–`V19`) и
`POST /lectures/generate/semester/{weekScheduleCycleId}` прогнано против того же живого приложения и
той же dev-БД, подтверждено прямым SQL-запросом к Postgres.

### Шаг 0. Миграции `V17`–`V19` применились к живой dev-БД

При старте приложения (`mvn spring-boot:run`) Flyway применил все три новые миграции к уже
существовавшей БД со сценариев 1–4 без ошибок:
```
Migrating schema "public" to version "17 - add end date to semesters"
Migrating schema "public" to version "18 - backfill semester end dates"
Migrating schema "public" to version "19 - make semester end date required"
Successfully applied 3 migrations to schema "public", now at version v19
```
`V18` корректно нашёл и заполнил `end_date = '2025-12-29'` ровно у тех трёх строк `semesters`, что уже
были в БД (все три — из `V15`, других строк `semesters` в dev-БД не оказалось: попытка создать новую
через API в Сценарии 1 давно падает `422`, см. врезку выше) — иначе последующий `V19`
(`SET NOT NULL`) не прошёл бы.

### Шаг 1. Новый тестовый студент, привязанный к У533 РПиС

Прежний тестовый студент из прогона 24.08.2026 уже удалён (см. выше), поэтому для этого прогона
зарегистрирован новый `ivanov.i` (`POST /students/register`, тело — как в Сценарии 2, Шаг 1) и привязан
к постоянной группе `У533 РПиС` из V15 (`PUT /students/{id}` под `ADMIN`, `groupId =
3d0f97bd-5c45-4f50-86a8-8506d2e16eeb`) — оба запроса `200`/`201`.

`GET /lectures/me` под свежим `STUDENT`-токеном **до** генерации — пусто, как и ожидалось (в БД пока нет
ни одной `Lecture` для этой группы):
```json
{"content": [], "totalElements": 0}
```

### Шаг 2. Сгенерировать весь семестр из цикла группы

`weekScheduleCycleId` группы `У533 РПиС`/`У530 РПиС` (по `pairs.week_schedule_cycle_id` через постоянный
Pair «История России» `8558192f-...` из V15) — `9645f60a-3431-4163-802e-4f094273272a`, привязан к
`Semester` `311ad18b-...` (`start_date = 2025-09-01`, `end_date = 2025-12-29` после `V18`), у которого в
этом цикле 56 разных `Pair` (28 `ODD` + 28 `EVEN`, все дни Пн–Пт).

```bash
curl -s -X POST "http://localhost:8023/api/v1/lectures/generate/semester/9645f60a-3431-4163-802e-4f094273272a" \
  -H "Authorization: Bearer <ADMIN_TOKEN>"
```

`200 OK`, тело — массив из **484** новых `LectureDto` (заняло ~2.9 c). Пример первого элемента:
```json
{
  "id": "2beee2ae-f477-4b56-a2ab-1601349bf593",
  "title": "История России",
  "scheduledTime": "2025-09-01T08:30:00",
  "durationMinutes": 90,
  "sourcePairId": "8558192f-b4d8-4077-bfe4-4b25fad33066",
  "groupIds": ["76677927-7dcc-4a12-981a-a31d59bf1697", "3d0f97bd-5c45-4f50-86a8-8506d2e16eeb"]
}
```
Прямо в Postgres:
```sql
SELECT count(*) FROM lectures;                      -- 484
SELECT count(DISTINCT source_pair_id) FROM lectures; -- 56 (все Pair цикла использованы)
SELECT min(scheduled_time), max(scheduled_time) FROM lectures;
-- 2025-09-01 08:30:00 | 2025-12-29 14:00:00 (ровно в границах [Semester.startDate, Semester.endDate])
```
Первая и вторая лекции того же `sourcePairId` — `2025-09-01` и `2025-09-15` (шаг 2 недели, т.к. пара
`ODD`-only) — подтверждает, что чётность недели действительно фильтрует даты, а не генерирует их каждую
неделю подряд.

### Шаг 3. `GET /lectures/me` после генерации — весь семестр разом

```bash
curl -s "http://localhost:8023/api/v1/lectures/me?page=0&size=3" -H "Authorization: Bearer <STUDENT_TOKEN>"
```
`200 OK`, `"totalElements": 285` (не 484 — 199 из 484 лекций принадлежат только группе `У530 РПиС`, не
`У533 РПиС`: у части `Pair` цикла `groups` — только одна из двух групп, не поток на обе, `/lectures/me`
корректно фильтрует именно по группе вызывающего студента). Первые 3 элемента страницы отсортированы
по `scheduledTime` по возрастанию: `2025-09-01T08:30`, `2025-09-01T10:10`, `2025-09-01T12:20` — три
разных занятия одного и того же дня (первого дня семестра), а не дубли одного предмета.

### Шаг 4. Идемпотентность и границы доступа

Повторный вызов `POST /lectures/generate/semester/{тот же ID}` под `ADMIN` → `200 OK`, тело `[]` — все
пара+дата уже сгенерированы Шагом 2, операцию безопасно вызывать повторно (например, после добавления
нового `Pair` в середине семестра — тогда вернёт только лекции нового `Pair`).

Тот же запрос под `STUDENT`-токеном (`ivanov.i`) → `403 Forbidden` — эндпоинт корректно закрыт
`@PreAuthorize("hasRole('ADMIN') or hasRole('TEACHER')")`, студент не может сам себе сгенерировать
расписание.

### Уборка

Тестовый студент `ivanov.i` (`d4960851-775f-4a90-8c1f-1a30805883b3`) удалён (`DELETE /students/{id}`,
`204`). Сгенерированные **484 лекции — оставлены в dev-БД навсегда**, в отличие от прошлого прогона: это
не тестовые данные, а реальный результат «опубликовать расписание на весь семестр», аналогично тому, как
остаются постоянными сами `Pair` из `V15`/`V16` — подтверждено повторным `SELECT count(*) FROM lectures`
(`484`) после удаления студента.

### Итог дополнения

`POST /lectures/generate/semester/{weekScheduleCycleId}` вживую превратил 56 `Pair`-шаблонов постоянного
расписания КТиЭ в 484 конкретные `Lecture` на весь осенний семестр 2025 (`2025-09-01`–`2025-12-29`), с
верной фильтрацией по чётности недели, идемпотентно и с корректной авторизацией (`ADMIN`/`TEACHER`
только). После этого `GET /lectures/me` у студента группы `У533 РПиС` вместо одной ручной лекции
(прогон 24.08.2026) отдаёт все 285 занятий его группы за семестр, отсортированные по времени — то, ради
чего изначально возник вопрос «зачем нам Lecture, если есть Pair» и его прямое следствие «а можем ли мы
сгенерировать весь семестр разом».
