-- ===================================
-- Room (аудитория) — на данном этапе не отдельная сущность (см. TARGET.md, где Room задумана как
-- Pair }o--o| Room), а простое опциональное поле-строка на Pair, пробрасываемое в Lecture при
-- генерации из шаблона Pair. Полноценная сущность Room — задел на будущее, если понадобится
-- отдельно управлять аудиториями (вместимость, конфликты бронирования и т.д.).
-- ===================================
ALTER TABLE pairs ADD COLUMN room VARCHAR(32);
ALTER TABLE lectures ADD COLUMN room VARCHAR(32);
